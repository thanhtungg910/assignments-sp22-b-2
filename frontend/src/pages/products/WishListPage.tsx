import React, { useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";

type Props = {};

const WishListPage = (props: Props) => {
	/* 	const wishListSele: String[] = useSelector((state: { wishList: String[] }) => state.wishList);
	console.log("🚀 ~ file: WishListPage.tsx ~ line 8 ~ WishListPage ~ wishListSele", wishListSele);
	const dispatch = useDispatch();
	useEffect(()=>{
		const fetchData=async()=>{
				wishListSele.map(item=>)
		}
		fetchData()
	},[])
 */

	return <div>WishListPage</div>;
};

export default WishListPage;
